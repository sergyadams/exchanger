{
  "buildCommand": "cd .. && (cd shared && npm install && npx tsc) && cd frontend && npm run build",
  "outputDirectory": ".next",
  "installCommand": "cd .. && npm install",
  "framework": "nextjs"
}
