{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build:shared && cd backend && npm run build"
  },
  "deploy": {
    "startCommand": "cd backend && chmod +x scripts/setup-db.sh 2>/dev/null || true && (bash scripts/setup-db.sh || npm run prisma:migrate:deploy && npm run prisma:seed) && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
