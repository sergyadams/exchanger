{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd .. && npm install && npm run build:shared && cd backend && npm run build"
  },
  "deploy": {
    "startCommand": "cp prisma/schema.production.prisma prisma/schema.prisma && npm run prisma:generate && npx prisma migrate deploy || npx prisma db push || true && npm run prisma:seed || true && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
