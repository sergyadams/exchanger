{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build:shared && cd backend && npm run build"
  },
  "deploy": {
    "startCommand": "cd backend && npm run prisma:generate && npx prisma migrate deploy || npx prisma db push && npm run prisma:seed || true && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
