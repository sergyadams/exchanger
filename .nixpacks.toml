[phases.setup]
nixPkgs = ["nodejs_18", "npm-9_x"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
  "npm run build:shared",
  "cd backend/prisma && cp schema.production.prisma schema.prisma",
  "cd ../.. && cd backend && npm run prisma:generate",
  "cd backend && npm run build"
]

[phases.start]
cmds = [
  "cd backend/prisma && cp schema.production.prisma schema.prisma && cd ../..",
  "cd backend && npx prisma migrate deploy || npx prisma db push || echo 'Migration failed, continuing...'",
  "cd backend && npm run prisma:seed || echo 'Seed failed, continuing...'",
  "cd backend && npm start"
]
